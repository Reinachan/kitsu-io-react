query GetAnimeList($slug: String!) {
	profile: findProfileBySlug(slug: $slug) {
		library {
			completed(mediaType: ANIME, first: 40) {
				nodes {
					media {
						posterImage {
							views {
								url
							}
						}
					}
				}
			}
		}
	}
}
